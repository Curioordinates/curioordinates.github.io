{
  "scripts": {
    "server": "python -m http.server --directory .",
    "generate": "npx tsx ./src/file-processor.ts"
  },
  "devDependencies": {
    "@types/node": "^20.12.3"
  },
  "dependencies": {
    "@types/jest": "^29.5.12",
    "jest": "^29.7.0"
  }
}
